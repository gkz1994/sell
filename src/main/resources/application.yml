spring:
    datasource:
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: admin
      url: jdbc:mysql://localhost/sell?characterEncoding=utf-8&useSSL=false&zeroDateTimeBehavior=convertToNull

    jpa:
      show-sql: true
    jackson:
      default-property-inclusion: non_null
server:
  context-path: /sell